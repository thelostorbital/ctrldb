package fixture

import (
	"charm.land/bubbletea/v2"
	"example.com/project/internal/fakes"
	"os"
	"os/exec"
	"syscall"
)

func violations() {
	_ = bubbletea.Quit
	_ = fakes.Value
	_ = exec.Command(`/bin/dash`, "-c", "echo unsafe")
	_ = exec.Command("C:\\Windows\\System32\\cmd.exe", "/c", "echo unsafe")
	_, _ = os.StartProcess("tool", nil, nil)
	_ = syscall.Exec("tool", nil, nil)
}
